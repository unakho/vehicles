--CREATE TABLE tschedule(id number(20) GENERATED BY DEFAULT ON NULL as IDENTITY,
--  scheduled_date timestamp NOT NULL,
--  constraint pk_schedule_id PRIMARY KEY(id));
--drop table if exists "assets_schema"."schedule";
create table if not exists "assets_schema"."schedule"("id" UUID not null,
           "scheduled_date" date, "vehicle_id" uuid not null, primary key("id"),
           foreign key("vehicle_id") references "assets_schema"."vehicle"
            ("id")) ;